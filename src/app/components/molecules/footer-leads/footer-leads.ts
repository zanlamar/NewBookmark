import { Component, Input } from '@angular/core';
import { Button } from '../../atoms/button/button';

@Component({
  selector: 'app-footer-leads',
  imports: [Button],
  templateUrl: './footer-leads.html',
  styleUrl: './footer-leads.css',
  standalone: true,
})
export class FooterLeads {

}
